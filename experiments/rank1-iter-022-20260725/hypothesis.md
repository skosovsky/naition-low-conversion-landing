# C22 — structured programme evidence salience

- Direct control: exact C7 runtime
  `d416b9e016ea8f2f7c30233cc60001c1a1132653`
- Best direct-control result: `30/101 = 29.7030%`, rank 2
- Latest result: C21 `27/101 = 26.7327%`, rank 2
- Hard pass: at least `41/101`, leaderboard rank 1, server and leaderboard
  agreement
- Mechanism: make the six unchanged, default-visible programme modules read as
  one complete sequence by rendering CSS counters `01…06` in the existing
  headings.
- Classification: causally new, truth-safe, low-capacity exploratory reserve.

## Local evidence

C10 produced the largest observed sensitivity: hiding supporting meaning moved
the exact C7 outcome from `30/101` to `15/101`. Normal simulator paths traverse
the programme before Basic selection. The partial frozen C20 proxies also
showed a directionally stronger programme-dominant subgroup than the
injury-dominant subgroup.

These signals establish exposure, not an eleven-order effect. The honest
planning range is `27–34/101`, most likely `30–31/101`; the estimated hard-pass
tail is only about `2–5%`.

## Isolated treatment

Restore exact C7 runtime and append one CSS-only mechanism:

- `.program-list` resets `course-module`;
- each existing `.program-module` increments it;
- each unchanged heading renders its sequence number through
  `.program-module h3::before`.

No programme word, DOM node, source order, interaction, form field, CTA,
authored JavaScript, API, SQL, event trigger or persisted conversion changes.

## Adversarial dissent

Two independent file-only reviewers returned `HARD_NO_GO`: no unblocked local
treatment has credible `+11` capacity. The only conditionally high-capacity
mechanism, later-group enrollment, would require owner-backed operational
commitments that are not in the repository.

Ann activates C22 only because the goal contract requires immediate,
causally-new iterations after a valid miss. C22 is not represented as a
predicted winner. Any valid result below the hard gate closes the entire
programme-salience family.
