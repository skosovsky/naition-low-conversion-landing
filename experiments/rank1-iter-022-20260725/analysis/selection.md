# C22 selection

Four independent file-only reviews used the saved C1–C21 artifacts:

- model forensics ranked `structured-program-evidence-salience` first among
  unblocked treatments (`27–34/101`);
- structural review proposed topic-order continuity (`27–33/101`);
- high-impact and adversarial reviews returned `HARD_NO_GO` for any honest
  `+11` claim and identified later-group enrollment as blocked by missing
  operational truth.

Ann selected the programme-salience reserve because it is the strongest
unclosed, truth-safe mechanism, is isolated to one visual sequence treatment,
and leaves the protected transaction path exact C7. The expected result remains
below the hard gate; this is a bounded exploratory iteration, not metric
gaming or a relabelled closed mechanism.
