# C23 high-impact reserve — HARD_NO_GO

```text
analysisMode          = file-only
networkBrowserMcpGit  = not used
siteMutation          = none
bestDirectControl     = C7 / 30 orders / 101 requests = 29.7030%
hardPass              = >=41/101 and leaderboard rank 1
requiredGainVsC7      = +11 persisted orders
requiredRescueVsC7    = 11/71 = 15.4930% of non-orders
C22StatusAtReview     = simulator running; no terminal result
selectedC23Candidate  = none
verdict               = HARD_NO_GO
```

## Decision

Do not prepare a C23 landing mutation from the current local facts.

C1-C21 already close the measured families around price/obligation,
reciprocal value, audience/outcome copy, learnability, programme-value detail,
credibility cleanup, form salience, content disclosure, progress, visual
primacy, calendar commitment, focus handoff, fixed-session adjacency, default
choice, plan semantics/comparison, application-state copy, visible-route count
and explicit plan-fit guidance.

C22 is already consuming the last truth-safe semantic reserve:
`structured-program-evidence-salience`. Its preregistered planning range is
only `27-34/101`, with a `2-5%` estimated hard-pass tail. If C22 misses, the
complete programme-salience family closes. If it reaches `>=41/101` and rank
1, C23 must not run.

The stable path remains:

```text
almost universal real Basic selection
→ stop before voluntary first-field focus
OR
→ name → phone → email → submit → persisted order
```

Observed voluntary form starters complete. The remaining unmeasured local
changes do not have credible `+11` capacity:

| Residual | Honest planning capacity | C23 disposition |
|---|---:|---|
| Reorder the existing injury cards for hero-to-detail continuity | about `27-33/101` | Causally new but insufficient |
| Remove the inherited reciprocal preview/reward | about `29-34/101` | Control cleanup; not a new mechanism |
| Another form/layout/CTA/plan semantic treatment | no supported lift | Closed-family repeat |
| Provider proof, deadline, SLA or flexibility copy | unknown | Unsupported without new product provenance |

## Only conditional high-capacity unlock

```text
mechanismId = date-flexible-open-enrollment
status      = BLOCKED_MISSING_OWNER_PRODUCT_CONTRACT
```

This is the only remaining mechanism with a plausible arithmetic route to
rescuing `11/71` C7 non-orders because it changes actual eligibility rather
than presentation of the fixed 15 August session.

The exact missing product fact is:

> The operator will accept and retain contact applications from people who
> cannot attend 15 August 2026 for a later, currently unscheduled Moscow group
> of the same full free course, and will contact those applicants when a real
> date is approved.

Before any source work, an attributable local owner artifact must record:

```text
willRetainLaterGroupApplications          = true
willContactApplicantsWhenDateExists       = true
laterGroupCourseRemainsSameFreeFullCourse = true
laterGroupCityRemainsMoscow               = true
fulfillmentOwner                          = named accountable person or team
confirmedAt                               = explicit UTC timestamp
```

Every value must be unconditional and operationally owned. Database storage of
name, phone and email does not prove future fulfillment.

If the contract appears after a valid losing C22 result, the conditional C23
treatment is one default-visible eligibility sentence on exact C7:

> Не подходит 15 августа? Оставьте заявку: сохраним контакты для следующей
> московской группы этого же бесплатного полного курса и свяжемся, когда
> назначим дату.

This conditional treatment must not add a date selector, alternate form,
future-date preference, seat, availability, SLA, scheduled-date implication,
new conversion, synthetic event, focus, prefill or submit path. The protected
backend still persists contacts only.

## Final gate

```text
if C22 >= 41/101 and rank1:
    stop; no C23
else if owner product contract is complete:
    C23 = date-flexible-open-enrollment
else:
    C23 = HARD_NO_GO
```

Without that owner-backed fact, another cohort would be a low-capacity
exploration, a renamed closed mechanism or an invented commercial promise.
