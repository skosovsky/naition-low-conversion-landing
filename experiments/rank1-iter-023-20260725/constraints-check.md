# C23 constraints check

- Root `STUDENTS.md` was read before implementation.
- The complete C22 programme-counter CSS block is removed.
- After identity normalization and removing the single injury-grid
  permutation, runtime is exact C7.
- `css/style.css`, authored `js/main.js`, `js/analytics.js`,
  `js/registration.js`, `api/submit.php`, `api/visit.php` and
  `sql/schema.sql` have exact C7 Git blob IDs.
- The deterministic bundle is byte-equal to C7 after normalizing only the
  experiment ID.
- The six complete `.injury-card` fragments preserve the C7 text multiset and
  remain default-visible inside the same `.injury-grid`.
- DOM, visual and accessibility order agree; no CSS `order` mechanism is used.
- `#registration-form`, `api/submit.php`, `name`, `phone`, `email`,
  `api/visit.php`, `.btn-register`, `.pricing-section`, `.program-module` and
  `.program-list` remain intact.
- All three pricing CTAs, analytics event names, provider mappings, triggers,
  API persistence and conversion semantics remain exact C7.
- No hidden content, synthetic click/focus/input/submit, user-agent branch,
  unsupported product claim or alternate conversion is introduced.
- Tests: `22/22` pass; JSON validation and `git diff --check` pass.
- Local PHP CLI is unavailable, but C23 changes no PHP syntax or executable
  PHP logic: only two static meta values and complete HTML article positions.
- Activation requires independent review PASS, exact local/remote SHA, one
  authenticated Redeploy, live-marker verification, pre-cohort `0/0`, and
  exactly one simulator invocation with `100/100` successful visits.
